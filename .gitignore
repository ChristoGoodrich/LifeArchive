# dependencies & build output (regenerated, never commit)
node_modules/
dist/
__pycache__/

# Capacitor native projects + web bundle (regenerated by CI / build-www.mjs)
/android/
/ios/
/www/

# local-only release helper (uses your machine's gh login; not for the public repo)
发布新版本.bat

# OS / editor junk
Thumbs.db
.DS_Store

# scratch files (commit messages / release notes written for gh)
*.tmp
.relnotes.tmp.md

# local Claude session data
.claude/
memory/
