# 更新日志 / Changelog

本项目所有版本的更新说明。每次发版都必须在这里新增一节，并同步到对应的 GitHub Release 说明。
格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/)，版本号遵循 [语义化版本](https://semver.org/lang/zh-CN/)。

---

## v1.8.0 · 重温·那年今日：回顾页 + 图片时间 + HyperOS 时间面板 — 2026-06-05

- **新增「那年今日 / 回顾」入口**：时间线顶部常驻「回顾」按钮；当今天能挑出旧记忆时，会显示「那年今日」「往月今日」或「随机重温」横幅，进 App 就能遇见过去的生活存档。
- **新增回顾页**：按「那年今日」「往月的今天」「随机翻牌重温」分区展示旧存档，复用现有时间线卡片，点卡片可直接进入详情页重温。
- **新增温暖的那年今日通知**：手机端只在有真实周年记忆时预排未来 14 天内对应日期 20:00 的提醒，点通知直达回顾页；桌面端继续用应用内横幅降级，不依赖原生推送。
- **「图片时间」替代「昨天此时」**：新建 / 编辑存档的时间面板现在会尝试读取所选图片的 EXIF / 原生拍摄时间，一键填入存档时间；没有拍摄时间的图片会给出提示，不会乱填。
- **存档时间面板改为 HyperOS 美学**：旧的 Material 感大色块日期 / 时间样式重做为轻玻璃卡片、柔和渐变状态区和圆角输入胶囊，更贴近应用整体视觉。
- **示例数据支持验收**：载入 demo 时会加入去年今天和上个月今天的记录，方便验证「那年今日」与「往月今日」链路。

> 本次不改 Supabase 表结构，新增的图片拍摄时间随现有 commit blob 一起同步。发布后请重点验证：时间线回顾入口 / 横幅、回顾页返回与详情跳转、选带 EXIF 照片后「图片时间」能设置存档时间、无 EXIF 时提示正常、Android 通知权限不对无周年用户乱弹。

## v1.7.0 · 引擎层闭环：复查提醒 + 可分享凭证 + 首存反馈 — 2026-06-05

- **新增本地复查提醒**：新建 / 编辑存档时可设置 7 / 30 / 90 天后或自定义天数复查；Android 首次设置提醒会请求通知权限，桌面端安全降级为仅保存提醒时间。
- **点通知直达现实对比**：同场景有两条以上正式存档时，通知会回到 Life Archive 并自动预选 compare 与 base；只有一条时落到详情页，方便再拍一张当前状态。
- **可分享凭证升级**：现实对比导出图现在给两张图分别盖上存档时间戳并加生成水印；详情页新增「导出此存档」，单条存档也能生成带时间戳、场景、清单和水印的图片凭证。
- **手机端导出接入系统分享**：Android / 原生端导出图片可直接调起系统分享面板；桌面端继续保留下载 / 长按保存的降级路径。
- **首存即有值**：保存正式存档后会提示本场景第 N 次、距上次 X 天、清单物品数；未设置提醒时提供一键 30 天复查入口。
- **空状态与分支提醒收口**：空时间线改为三步引导，保留「载入示例数据」入口；分支到期日也会在手机端重排为本地提醒。

> 本次新增本地通知能力，Android 首次设置复查提醒时会请求通知权限；不改 Supabase 表结构，提醒字段随现有本地/云端数据 blob 同步。发布后请重点验证：桌面端无原生插件时报错为 0、保存存档和导出图片正常；安卓端设置短周期提醒、点通知进入现实对比、删除带提醒存档后不再误推、单条/对比导出图都带时间戳水印、空时间线显示三步引导。

## v1.6.3 · 应用图标焕新 + 开屏彻底去抖 + 键盘白色遮罩修复 + 时间线防溢出 + 存档时间统一 — 2026-06-05

- **应用图标焕新，和开屏统一**：桌面 / 启动器图标重画为与开屏画面完全一致的蓝紫渐变「卡片堆叠」标志——浅色磨砂玻璃底，上面三张由浅到深、向左下微微错开的层叠卡片。以前图标是紫色底 + 白色扇形卡片，和开屏看到的标志对不上；现在点开应用前（图标）和打开瞬间（开屏）看到的是同一个标志。Windows `.ico`、Android 自适应图标（前景 + 背景）与启动图均已重新生成。
- **开屏彻底去抖**：开屏品牌画面只做淡入、不再做任何上下位移动画；并且改为**等到安全区（状态栏高度）真正注入、顶栏高度落定之后**才揭开遮罩（带最短展示与超时兜底）。以前只是延长了覆盖期，安全区偶尔来得晚就会在遮罩淡出后才把内容往下顶一下，造成「有时候」进入应用上下抖动；现在这次高度沉降被完整藏在不透明的开屏下面，抖动消除。
- **修复键盘弹出输入区整片白色遮罩**：Android 原生层同步调整 WebView 与父容器高度、禁用输入法全屏抽取，并在 IME 动画帧里强制重绘，HyperOS / 搜狗键盘下搜索框和时间线卡片都能正常显示。
- **修复新建存档后时间线横向飞出屏幕**：时间线列表、日期组、轨道、卡片和图片容器都加上 `min-width:0 / max-width:100% / width:100%` 约束；路由渲染回时间线时同步清掉 `.view`、`html`、`body` 的横向滚动漂移。
- **统一「存档时间」控件风格**：新建 / 编辑存档不再直接露出原生 `datetime-local` 控件，改为应用内同款圆角选择按钮；点击后弹出锚定面板，可选择日期、时间，也可快捷填入「现在」或「昨天此时」。
- **取消 Windows 免安装便携版**：Windows 发布目标只保留支持自动更新的安装版，避免用户下载到不会自更新的 portable 包。

> 本次为图标 / 开屏视觉统一 + 移动端 UI 稳定性修复，无数据结构变更，已有存档无需迁移。本版为 v1.6.3 的覆盖发布（重新构建并替换同版本的发布产物）。发布后请重点验证：桌面 / 启动器图标外观、冷启动开屏到首页无抖动、时间线搜索框唤起键盘、保存新存档返回时间线、存档时间控件弹层、Windows 安装包自动更新。

## v1.6.2 · 现实对比控件优化 + 开屏加长去抖动 + 键盘/宽度兜底 — 2026-06-04

- 🎛 **现实对比操作区优化**：「AI 解读变化」与「导出对比卡片」按钮和它们下方的对比结果之间补上了间距（原来只有 2px，贴得很紧）；同时把「导出对比卡片」从一个带文字的按钮**简化成一个紧凑的分享图标**，与主操作「AI 解读变化」并排，界面更清爽。
- 🎬 **开屏加长 + 消除入场抖动**：上个版本把开屏缩短后，首屏的异步安全区插值 + 顶栏重测沉降被暴露出来，进入应用时会上下抖一下。现在把开屏时长适当加长（约 1.1s），并在开屏覆盖期内多做一次顶栏重测，让这些布局沉降都发生在开屏盖住屏幕的时候，入场不再跳动。
- ⌨️ **再次修复键盘弹出时输入框下方的白块（找到真正祸首）**：上个版本只改了 `--keyboard-inset`，但 `.view` 的 `padding-bottom` 里还有**另一个大值**——`--safe-area-inset-bottom`。`@capacitor-community/safe-area` 在部分安卓机型上会把 IME（输入法）高度也折算进底部安全区，于是键盘弹出时这个变量膨胀到接近一个键盘高，`.view` 底部又被多垫一整块，正是那片白色遮罩。本版在 `body.kb-open` 时把 `.view` 的底部安全区清零（实测内容区底部内边距从 **422px → 82px**，键盘收起后自动恢复）。另加两道低风险兜底：① 给 `html` 也加主题背景，避免 WebView 在 IME 缩放瞬间露出默认白底；② 键盘交互后做一次不可感知的 1px 滚动回弹，强制 WebView 重绘 IME 动画留白的区域。> 注：桌面模拟中网页布局本身渲染完全正常（搜索框→筛选条→卡片顺序正确），所以此前判断为原生层问题；本次定位到是原生安全区变量膨胀污染了网页的 padding 计算。
- 📐 **新建页宽度兜底**：给新建 / 编辑存档页加了 `min-width:0` 与图片 `max-width:100%` 约束，任何子元素（图片、物品行、预览）都不会把卡片撑得超过手机宽度，减少反复出现的安卓横向漂移。

> 本次为体验优化 + 兜底加固，无数据结构变更，已有存档无需迁移。说明：键盘残留白块在桌面无法复现（网页布局经模拟验证是正确的），本版的两道兜底针对最可能的原生成因，若真机上仍存在，请在白块出现时截图反馈，下个版本会带上设备诊断信息进一步定位。发布后请在安卓真机重点验证：现实对比按钮间距与分享图标、进入应用不再抖动、键盘弹出时输入框下方是否仍有白块、新建存档页宽度。

## v1.6.1 · 修复键盘白屏 + 固定开屏画面 + 云 SDK 完整性校验 — 2026-06-04

- ⌨️ **修复键盘弹出时输入框下方一大片白色空白（“白色遮罩”）**：根因是部分安卓 WebView（如 HyperOS）在系统用 `resizeOnFullScreen` / `adjustResize` 把网页缩小避让键盘后，`window.innerHeight` 仍停留在键盘弹出前的旧值；键盘遮挡量按 `innerHeight − visualViewport.height` 计算就被算成“整整一个键盘高度”，于是 `--keyboard-inset` 又给 `.view` 垫了一整块内边距，叠在已经缩小的视图下方，看起来就像被白色遮罩盖住。现在改用**布局视口的实际高度**（`min(innerHeight, documentElement.clientHeight)`，并计入 `visualViewport.offsetTop`）来计算遮挡量，缩小后的视图不再重复垫白。
- 🎬 **开屏动画改为固定画面**：去掉底部的加载进度条，开屏只展示一个固定的品牌画面（应用图标 + 名称 + 标语「给现实生活装一个 Git」），配一次轻微淡入；开启系统「减少动态效果」时仍为快速淡出。原生启动图负责的无白屏冷启动保持不变。
- 🔒 **云同步 SDK 加上完整性校验（SRI）**：Supabase SDK 由浮动版本 `@2` 改为**锁定到精确、不可变的包内静态文件** `@2.107.0/dist/umd/supabase.js`（浮动别名是 jsDelivr 实时再压缩的动态文件，官方明确不建议对其用 SRI），并加上 `integrity`（sha384）与 `crossorigin`。这样即使 CDN 被篡改或遭中间人攻击，浏览器也会在脚本运行前直接拒绝加载，避免恶意脚本借渲染层读到本地存档。

> 本次为修复 + 安全加固，无数据结构变更，已有存档无需迁移。发布后请在安卓真机重点验证：键盘弹出时输入框下方不再出现大片空白、底部输入框聚焦正常；冷启动开屏为固定画面、无加载条；以及登录后云同步（拉取 / 上传）仍正常工作（SDK 加 SRI 后请确认仍能正常加载）。

## v1.6.0 · 回滚工作台 + 开屏动画 + 移动端体验收口 — 2026-06-04

- **回滚升级为「回滚工作台」**：回滚页会同时展示当前状态与目标状态，并给出「少了 / 多了 / 数量变化」摘要；恢复步骤支持勾选，进度会自动保存在本机，退出后再回来可以继续。
- **新增「生成恢复存档」**：回滚不覆盖旧历史，而是把目标状态写成一条新的正式存档，并记录 `rollbackTargetId / rollbackFromId`；时间线卡片和详情页会标记这条存档来自哪次回滚。
- **多图回滚更实用**：目标存档若有多张图片，回滚页会显示封面与缩略图条，不再只能看单张参考图；饮食类仍保留「照着再记一笔 / 预存档」逻辑，不强行套物品回滚。
- **修复多图 / 长图在时间线的展示**：长截图、竖向长图会在时间线压成紧凑预览，避免一张图撑满整屏；详情页仍按原比例完整查看。缩略图也补充尺寸信息，进一步减少新建多图存档后的跳变。
- **继续收口移动端输入框和新建页宽度**：手机端新建 / 编辑存档页更贴近可用宽度；物品行、底部按钮、键盘弹出时的焦点滚动和 Android WebView 偶发横向漂移都做了加固。
- **新增开屏动画**：保留 Android 原生启动图负责无白屏冷启动，再用 Web 首屏短动画完成品牌过渡；系统开启「减少动态效果」时会自动降级为快速淡出。

> 本次是功能性升级，数据结构向后兼容，只新增回滚来源字段和本机回滚进度缓存。发布后请重点验证：回滚工作台勾选进度、生成恢复存档、长图 / 多图时间线展示、新建页宽度、安卓键盘输入框聚焦，以及冷启动开屏动画。

## v1.5.3 · 移动端新建存档与现实对比 / 回滚体验修复 — 2026-06-04

- **修复移动端新建 / 编辑存档页宽度**：手机上表单卡片内边距更紧凑，输入框、照片区、AI 按钮和场景选择能更自然地使用可用宽度，不再显得被挤窄。
- **继续加固输入框聚焦**：滚动兜底会把顶部栏高度算进可视区，时间线搜索、新建存档说明、时间等靠上的输入框不会再被顶到顶部栏下面。
- **整理现实对比操作区**：删除低价值的“复制文本”按钮，只保留“AI 解读变化”和“导出对比卡片”；两个按钮高度统一，排布更稳定。
- **移除用户侧“敏感度”滑杆**：视觉对比继续使用内部固定阈值，显示模式控件独占一行，界面更清爽。
- **清理回滚页下拉箭头**：场景和时间描述选择器仍然可点选，但不再显示蓝色小箭头，回滚选择区更干净。
- **修复新存档添加后的时间线画面比例 / 跳变**：所有拍照、相册、多图、截图、粘贴入口都会记录缩放后的图片尺寸；多图首图兜底也能预留正确比例，时间线卡片不再等图片解码后突然放大。

> 本次主要修复 Android / 移动端 UI 体验和现实对比操作密度；数据结构向后兼容，仅继续使用已有的 `photoW/photoH` 尺寸字段，已有存档无需迁移。发布后请重点验证：新建存档页宽度、键盘弹出时输入框可见性、现实对比操作区、回滚选择器，以及新拍 / 新选图片出现在时间线时的比例稳定性。

## v1.5.2 · 删除可彻底同步 + 时间线放大修复 + 多图缩略图 + 设置分组 + 扁平图标 — 2026-06-04

- ☁️ **彻底修复「删了又被云端拉回来」**：之前删除存档后，下一次云同步会因为云端还存着这条记录、按 id 合并时又把它下载回来，导致删不干净。现在新增**删除墓碑（tombstone）**并参与云同步：本机删除存档后，会在所有已登录设备和云端**一起删除**；「清空全部」同样会同步清空云端。若某条存档在云端有比删除更新的编辑，则仍会保留（**编辑优先于过期删除**）。
- 🖼 **修复「新建存档后时间线上的存档会放大」**：根因是卡片没有为图片预留高度，刚建的封面图解码完成时卡片会从很矮**突然撑大**（实测约 +500px 的跳变）。现在保存封面图时会**记录图片像素尺寸**，时间线 / 详情页据此**提前预留图片框**，照片解码完成时不再跳大。旧存档再次编辑保存后同样获得该尺寸。
- 🧩 **多图存档在时间线新增「封面下缩略图条」**：多图存档过去只显示封面 + 一个张数角标，其余图只能进详情看。现在封面下方多出一排**可横滑的小缩略图**，直接在时间线看到这条存档的其他照片（超过 8 张显示「+N」）；点卡片仍可进入详情看完整图库。
- 📐 **修复新建 / 编辑存档页面宽度**：桌面端原来内容宽到 1040px，单行输入框被拉到将近 1000px，过宽不协调。现在改为更舒适的**居中宽度（约 680px）**，手机端不受影响。
- ⚙️ **设置界面重新分组、重新排序**：账号与云同步**置顶**，关于（版本 / 检查更新 / 更新日志）**置底**，中间按「通用（外观语言、AI 识别）」「数据」分组，并加上**分组小标题**，层次更清晰。
- ⌨️ **继续加固移动端输入框**：在系统视口事件之外再监听 `window resize`，并补一个较晚的回正帧，键盘弹出动画较慢的机型也能把当前输入框滚动回可见区。安卓底层仍是 `adjustResize + Keyboard.resizeOnFullScreen` 的既有方案。
- 🎨 **应用图标底色改为扁平纯色**：去掉原来的斜向渐变 + 高光 + 暗角，换成干净的蓝紫纯色底，**卡片玻璃质感保留**；桌面与安卓图标、启动图已重新生成。

> 数据结构向后兼容：仅新增 `photoW/photoH` 与删除墓碑字段，已有存档无需迁移；删除墓碑会随云同步在设备间生效。发布后请在安卓真机重点验证：删除存档后再同步不会复活、新建存档时间线卡片不再放大跳变、多图缩略图条显示正常、输入框聚焦时顶栏不被顶飞。

## v1.5.1 · 新建存档体验整理 + 热力图去下拉符号 — 2026-06-04

- **顶栏毛玻璃底部的细线已移除**：滚动内容和顶部模糊层之间不再有割裂感。
- **新建 / 编辑存档页面宽度优化**：桌面端会使用更宽的响应式内容宽度，不再被全局阅读宽度压得偏窄，手机端仍保持单列适配。
- **照片预览支持删除并统一按钮尺寸**：照片预览右上角新增删除按钮，选错封面图时可以直接清空后重选；「选择多张图片」按钮调整为和「AI 识别照片」同级的大按钮。
- **新建 / 编辑存档支持修改存档时间**：新增「存档时间」字段，允许补录真实发生时间；新增存档的同场景链路会按填写的存档时间寻找上一条正式存档。
- **预存档按钮更干净**：新建存档底部的「预存档」按钮去掉图钉表情，只保留文字。
- **存档热力图标题清理**：保留左右切换月份箭头，但删除月份标题下方的下拉符号，日历头部更干净。

> 本次为前端体验和时间录入修复；数据结构保持兼容，已有存档不需要迁移。

## v1.5.0 · 五项同级导航 + 输入框顶飞修复 + 顶栏模糊回归 + 纯月历热力图 + 现实对比 AI/趋势/导出 — 2026-06-04

- 🧭 **底栏五项完全同级**：时间线 / 现实对比 / 新建 / 回滚 / 分支现在都作为 peer tab 处理。tab 之间切换不再写入可侧滑返回的页面历史，因此不会再出现“现实对比、回滚、分支像时间线子页面一样侧滑回时间线”的层级错觉；设置、详情、热力图、更新日志等子页面仍保留正常返回层级。
- ⌨️ **全力修复安卓输入框顶飞问题**：确认 Capacitor 8 的 `Keyboard.resize` 对 Android 不生效后，改用 Android 真正读取的 `Keyboard.resizeOnFullScreen: true`，并保持 manifest `adjustResize`。网页层移除会双重缩放的 `interactive-widget=resizes-content`，同时保留 `visualViewport` + 原生键盘高度的有界兜底：可视区缩小时信任可视区；可视区没缩但原生事件给了键盘高度时，用键盘高度避让输入框。
- 🧊 **顶栏毛玻璃模糊回归并加强**：顶栏背景改成更通透的磨砂玻璃（更强 blur、更低不透明度、底部羽化和细高光），原生端让内容从顶栏下滚过，确保 backdrop blur 真正有内容可模糊。
- 🗓 **存档热力图改成纯月历**：不再分近三月 / 近半年 / 近一年；一次充分展示一个月的数据。可用左右圆形按钮翻月，也可点月份标题打开年/月选择器。
- ⭐ **热力图圆点可交互**：有存档的日期圆点可以点按，弹出当天存档数量，并列出当天星标存档；点星标存档可直接进入详情。
- 🔍 **现实对比按规划全面增强**：新增双图 AI 解读变化（复用用户自己的免费智谱 key，无 key 时本地对比照常可用）、跨场景任意两版对比、相邻存档照片 `changedPct` 趋势曲线（缺照片时用清单变化兜底）、“最常消失 / 最稳定 / 最常新增”洞察、饮食场景重复食物高亮与可选 AI 热量估算。
- 🖼 **对比导出 / 分享补齐**：现实对比可生成 before/after 对比卡片图，也可复制文字总结；近似物品归并（例如“可乐 / 可口可乐”）会同步用于界面、导出图片和复制文本。
- ✏️ **文案统一**：新建存档内「存为计划」改为「预存档」，「提交存档」改为「存档」，「物品场景」改为「物品」；热力图入口按钮里的小方块也改为小圆点，和日历圆点风格统一。

> 本次涉及 Android 输入法、底栏返回层级和视觉对比等核心交互。发布后请在安卓真机重点验证：新建 / 编辑存档输入框聚焦时顶栏不被顶飞，底栏 tab 不能侧滑回时间线，热力图点按日期弹出当天统计，现实对比的 AI/趋势/导出控件可正常使用。

## v1.4.2 · 热力图改月历 + 时间线扁平化 + 日期吸顶完全贴合 — 2026-06-04

- 🗓 **「存档热力图」改成真正的月历**：原来的横向贡献图在手机上会显示不全、要左右拖。现在改成日历格式——每个月一块日历，用**圆点**深浅表示当天存档多少；7 列正好铺满手机屏幕，**再也不会被裁切**。今天有高亮圈，未来日期淡显，每月还标注当月存档总数；范围支持近三月 / 近半年 / 近一年。
- 🧵 **左侧时间线扁平化重做、更规整**：主线从带光晕的渐变粗线收细成一条**柔和的扁平直线**（去掉发光）；存档节点从带高光/外发光的渐变圆点改为**干净的纯色实心圆点**，并去掉多余的连接小短线；主线、日期圆点、卡片节点统一对齐到**同一条竖线**上，更符合澎湃的扁平美学。
- 📌 **日期吸顶衔接彻底重做（完全贴合）**：把日期分组改成 flex 容器，所有间距移到内容盒之内，并让每个日期标题等高、零外边距。这样滚到下一天时，新日期会**完全贴合**着旧日期把它顶替出去——**紧贴、零缝隙、零重叠**，交接随手指顺滑推进，不再出现旧版「不丝滑」的跳变，或旧日期先飘走、中间露出约 30px 空隙的瑕疵。
- 🧰 **顺手修复缓存问题**：给 `styles.css` / `store.js` / `diff.js` / `app.js` 都加上 `?v=` 版本号查询串（原先只有 `version.js` 有）。确保每次自动更新后，样式和脚本的缓存**一定会刷新**，不会再加载到上一版的旧 CSS / JS。

> 本次为纯前端 UI 改动，数据与同步不受影响。发布后可在安卓真机滚动时间线、查看顶栏「存档热力图」日历，确认日期吸顶交接顺滑无空隙、热力图完整显示。

## v1.4.1 · 底栏归位 + 锚定下拉 + 时间线美化 + 票据存档 + 日历热力图 — 2026-06-04

- 🧭 **导航栏移回底部**：把 v1.4.0 改到顶部的标签栏改回底部固定栏（HyperOS 风格，中间是扁平、不外凸的加号）。输入时底栏自动隐藏，不会再挡住键盘；顶部毛玻璃在键盘打开时依旧保留。
- ⌨️ **输入框问题继续处理**：底栏归位后重新接上「键盘弹出即隐藏底栏」逻辑；焦点兜底仍只认 `window.visualViewport` 这一个真实可视区（见 v1.3.1），有界滚动不会把页面顶飞。**仍需在安卓真机重点验证输入体验。**
- ⬇️ **选项弹框改为「锚定下拉菜单」**：场景、版本等所有选择弹框不再从屏幕底部滑出，而是在你点击的控件旁边就地展开（空间不够会自动向上翻），更符合「点哪弹哪」的直觉；照片来源（拍照 / 相册 / 截图 / 粘贴）也从点按位置弹出。
- 🗓 **日期标题更轻盈、交接更顺滑**：把吸顶的整条毛玻璃横条改为两枚轻盈的悬浮胶囊（日期 + 数量），吸顶时不再突兀，滚动时日期之间的过渡更自然。
- 🧵 **左侧时间线美化**：主线改为带柔光、两端淡出的渐变竖线；每个存档节点改为带高光的蓝紫渐变圆点，配柔和外发光和短连接线，整体更精致。
- 🎫 **新增「票据」场景分组**：票根票券、购物小票、发票账单，方便归档电影票、车票、演出票、购物小票、发票等。
- 📊 **新增「存档热力图」**：顶栏左侧新增九宫格入口按钮，按日历用圆点深浅展示每天的存档数量（类似贡献图但按日历排布），并附 总存档 / 活跃天数 / 连续天数 / 单日最多 统计，支持近三月 / 近半年 / 近一年切换。

> 发布后请在安卓真机重点验证：底部导航栏位置与键盘弹出时的隐藏、选项下拉菜单的弹出位置、新建存档输入框聚焦、「票据」场景归档，以及顶栏热力图入口与日历点阵显示。

## v1.4.0 · 顶部导航 + 连续时间轴 + 现实对比增强 — 2026-06-03

- 📷 **新建存档照片入口收敛**：原生端只保留「拍照 / 相册」两个选项；相册默认多选。「选择多张图片」辅助按钮改为选中封面图后才出现，避免空状态时控件过早占位。
- 🧭 **移动端选项卡回到顶部**：手机端导航从底部栏改为顶部粘性标签栏，减少和键盘、系统手势区域的冲突；顶部毛玻璃在键盘打开时不再消失。
- 🧵 **时间线连续且日期粘顶**：真实存档改为一条不断开的左侧主线；日期标题粘在内容顶端，同一天内滚动保持日期可见，切到下一天时自然滑走并替换。
- ⌨️ **输入框聚焦继续加固**：焦点兜底不再只依赖键盘覆盖高度；即使 WebView 已经 resize，也会把当前输入框在 `.view` 或弹层滚动容器内带回可见区域。
- 🔍 **现实对比大幅增强**：存档详情页新增「与上一版对比」入口，自动用当前存档和父存档进入 diff；对比页新增热力图、左右滑块、淡入闪烁三种视觉模式。
- 🎚 **更可控、更少误报的 diff**：热力图敏感度变为可调滑杆；清单对比会归一化大小写、全半角以及尾随数量/单位，减少「牛奶」和「牛奶1盒」被误判成不同物品。

## v1.3.2 · 时间线瀑布流 + 多图存档 + 饮食计划 + 输入框再加固 — 2026-06-03

- 🧵 **时间线重排为统一的单列瀑布流**：存档卡片改成「图片在上、内容在下」的统一版式，左侧时间线加入日期节点、连接短线和更清晰的竖向轨道，存档之间的先后关系更明显。
- 🖼️ **存档图片不再裁切**：时间线卡片、详情页主图、回滚参考图和新建存档预览都改为按图片原始比例展示；横图、竖图会自然撑开卡片，不再被强行裁成固定方块。
- 📷 **新建存档支持多图**：照片区新增「选择多张图片」按钮，文件选择器支持一次多选；第一张作为封面，其余图片保存为附件，并在详情页按原比例预览。
- 📌 **新增「存为计划」**：新建存档可以先保存为计划 / 预存档，计划会浮在时间线顶部，不参与现实对比、回滚或真实存档链；完成后点「完成」再转为正式存档。
- 🍽️ **饮食 / 零食回滚改成更符合语义的“照着再记一笔”**：零食饮品、早餐、午餐等饮食类不再强行生成回滚步骤，而是提供快速复记入口；也可以先存为想吃 / 要点的计划，真正吃到后再标记完成。
- ⌨️ **继续加固 Android 输入框聚焦问题**：真机原生环境下锁住 document，只让内容区滚动，避免输入法把顶栏或整页一起顶飞；仍保留 `visualViewport` 的有界兜底滚动。

> 发布后请重点验证 Android 真机：新建 / 编辑存档输入框聚焦、多图选择、计划完成、零食饮品回滚页，以及不同横竖比例图片在时间线里的展示。

## v1.3.1 · 键盘根因再修 + 新建即自动同步 + 星标存档 + 扁平加号 + 图标提质 — 2026-06-03

- ⌨️ **再修安卓「聚焦输入框」问题——这次清掉了互相打架的根因**：之前同时开了三套「键盘避让」——网页层 `interactive-widget=resizes-content`、Capacitor `resize:'body'`、再加上用原生 `keyboardHeight` 撑底部空间，三者叠加把布局缩了两三次，所以输入框还是会乱跳。现在统一只认 `window.visualViewport` 这一个真实可视区：把 `@capacitor/keyboard` 改成 `resize:'none'`（插件不再插手），底部留白只按「键盘真正盖住内容的高度」来算（现代 WebView 下浏览器已自动收缩布局，这个值≈0，不再重复留白），并保留**有界**兜底滚动（永远不会把页面顶出屏幕）。**仅影响安卓，桌面端不变；请在真机再验证一次输入体验。**
- ☁️ **新建存档后自动同步到云端**：配置并登录了云同步（Supabase）的用户，每次保存新存档会自动在后台同步一次，并给出「☁ 已自动同步到云端」提示；编辑存档、加/取消星标也会静默同步。离线时自动跳过、仅存本地，联网后可手动同步。
- 🔀 **同步合并改为「谁更新用谁」**：以前同步遇到同一条存档时一律以云端为准，会把刚在本机做的修改/星标覆盖掉。现在按 `updatedAt` 时间戳取最近修改的版本，本机的新改动不会再被旧的云端数据冲掉（编辑、星标都更稳）。
- ⭐ **新增星标存档，标记重要时刻**：在时间线卡片右上角点星，或在存档详情页点「☆ 标记重要」即可标星；星标卡片会显示金色星标和金色时间线节点；时间线顶部会出现「⭐ 重要」筛选，一键只看重要存档。
- ➕ **底栏中间加号改为扁平澎湃风**：去掉了又大又亮、还会顶出底栏的悬浮玻璃球，换成**收在底栏内**的简洁圆角方块加号——扁平、干净、克制，符合澎湃美学。
- 🎨 **图标与 Logo 配色提质**：把图标底色从偏青蓝/深黑收敛为更通透的蓝→紫品牌色，卡片光泽与亮边也调淡，整体更简洁、更高级；应用内 Logo 改为同一套渐变的层叠卡片。桌面图标 / 安卓自适应图标 / 启动图 / 应用内 Logo 全部同步更新。

> 键盘修复属于安卓原生行为，安装后请在真机上重点验证：聚焦各输入框、新建/编辑存档、登录云同步后新建存档是否自动同步。

## v1.3.0 · 移动端卡死修复 + 分支复盘闭环升级 — 2026-06-03

- 📱 **修复手机端严重卡死风险**：设置页存储用量不再同步 `JSON.stringify` 整个照片/附件库；时间线和分支列表改为分段渲染，详情页图片预览限制首屏解码数量，避免大数据量时把 Android WebView 主线程打满。
- ⌨️ **键盘兜底滚动降载**：`visualViewport` / Keyboard 事件触发时改为节流处理，并取消连续 `smooth` 滚动，降低输入框聚焦时排队滚动导致的卡顿风险。
- 🔀 **分支复盘闭环升级**：分支新增复盘到期日、待复盘导航角标、未选择 / 待复盘 / 已到期 / 已复盘状态、决策把握和标签。
- 🔎 **分支可发现性升级**：分支页新增搜索、状态筛选、标签筛选和洞察卡，可查看总数、待复盘、已复盘、平均评分、会重选率和预测命中率。
- 🧾 **分支详情页**：列表卡片瘦身，选择、复盘、编辑、删除收进二级详情页；复盘时可逐条记录预测是否命中和实际结果。
- 🔗 **打通 Git 隐喻**：复盘后的分支可生成一条时间线存档，存档与分支详情之间支持双向跳转。
- 🤖 **Android 打包一致性加固**：同步 `MainActivity` Java 包路径、Android string resources、Gradle `appId` / 版本号和 manifest 行为到 `com.lifearchive.app`，避免原生工程残留旧包名。

> 本版本重点处理手机端主线程卡死风险和分支功能闭环。发布后建议在 Android 真机重点验证：进入设置页、打开分支页、选择到期分支、保存复盘并生成存档。

## v1.2.3 · 移动端版本同步 + 多图附件 + 分支决策增强 — 2026-06-03

- 📱 **修复 Android 打包版本不同步风险**：打包脚本现在会同步 `appId`、`namespace`、`applicationId`、`versionName`、`versionCode` 和 `www/` 资源；避免手机覆盖安装后仍显示旧版本或装到旧包名下。
- 🖼️ **单个存档支持多个文件 / 图片附件**：附件入口改为「文件 / 图片」，详情页会直接预览图片附件；没有主照片时，时间线可用第一张图片附件作为缩略图。
- 🏷️ **明确 `okc08` 这类短码含义**：详情页不再只裸露短码，改为「存档号 okc08」，说明它是 commit id 的短显示，不是场景、标签或设备信息。
- 🔀 **分支决策支持 2 到 4 个选项**：创建表单默认收起，新增/移除选项更轻；旧的两选项分支保持兼容。
- ✏️ **已有分支可编辑**：点击「编辑分支」会回填问题、选项和预期结果；保存时保留选择/复盘状态，选项数量变少导致旧选择无效时会自动清空选择。
- 🗑️ **删除分支前确认**：分支删除不再一键直接消失，确认后才真正删除。
- 🖼️ **README 截图与说明刷新**：重新生成展示图，并把说明更新到当前的多附件和增强分支能力。

> Android 真机验证时，请确认设置页显示 v1.2.3；如果仍显示旧版本，优先检查是否安装了旧包名 `com.realitygit.app` 和新包名 `com.lifearchive.app` 两个应用。

---

## v1.2.2 · 桌面存档增强 + 移动端交互打磨 — 2026-06-03

- 🖥️ **桌面端「新建存档」独立为 tab 栏右侧按钮**：桌面不再和手机一样把新建入口挤在中间；浏览类 tab 继续成组展示，新建存档作为明确的主操作按钮放到右侧。移动端仍保留底部中间悬浮加号入口。
- 📎 **存档支持真实文件附件**：新建/编辑存档可以添加多个文件，文件会随存档一起保存在 IndexedDB 中；详情页可查看附件列表并直接下载。学习资料、作业、报告、课件等现在可以和照片/备注一起组成完整存档。
- 📸 **桌面端实时截图存档**：Electron 桌面版在新建存档里新增「截取当前屏幕」，会短暂隐藏应用窗口并保存当前屏幕画面，适合快速保存桌面、作业、网页或资料状态。
- 🖼️ **桌面端支持直接复制/粘贴图片**：新建存档可从剪贴板读取图片，也支持 Ctrl+V 和拖拽图片到照片区域，不必每次打开文件选择器。
- ⌨️ **再次加固移动端键盘问题**：启用 Capacitor Android `resizeOnFullScreen` workaround，并用键盘事件返回的 `keyboardHeight` 驱动页面底部空间和输入框可见性兜底，降低 edge-to-edge WebView 下输入框被顶飞或遮挡的概率。
- ✨ **重做移动端加号、照片来源面板与筛选栏阴影**：中心加号改为更克制的玻璃质感圆钮；拍照/相册/截图/粘贴面板改为统一的应用内 action sheet；时间线筛选栏去掉脏边阴影。
- 🎨 **图标底色继续打磨**：调整图标生成脚本中的底色深度和光照，让桌面图标、Android adaptive icon、启动图保持同一套更高级的视觉基调。
- 🖼️ **README 截图已重新生成**：更新 `docs/screenshots/` 下 4 张展示图，避免 README 继续展示旧的或异常截图。

> 本版同时涉及 Android 键盘行为和 Electron 截图桥，发布后建议分别在真机与桌面安装包里各验证一次。

---

## v1.2.1 · 键盘根因修复 + 液态玻璃图标 + 二级页侧滑返回与丝滑动效 — 2026-06-03

- ⌨️ **再次修复安卓「聚焦输入框时整页被顶到看不见」——这次从根因下手**：根因是 App 为读取全面屏安全区开启了 edge-to-edge，而安卓在 edge-to-edge 下会**忽略 `adjustResize`/`adjustNothing`**、退回 adjustPan 把整页（含顶栏）一起顶上去——所以之前换原生模式都没用。改为在**网页层**处理：viewport 启用 `interactive-widget=resizes-content`，由浏览器收缩布局视口、自动保持输入框可见，并加了一个基于 `visualViewport` 的**有界兜底**（绝不会再把页面滚出屏幕）。**仅影响安卓，桌面端无变化。**
- 🎨 **图标换回卡片堆造型 + 液态玻璃质感**：保留更有「存档感」的层叠卡片，配色优化为更通透的蓝紫，并加入半透明磨砂、顶部高光泽面、柔和投影与折射亮边的「液态玻璃」光影。桌面图标 / 安卓自适应图标 / 启动图 / 应用内 Logo 全部更新。
- ➕ **底栏中间「新建」按钮去掉文字**，改为更突出的悬浮「＋」圆形按钮，带玻璃高光。
- ↩️ **二级页支持系统级侧滑返回**：设置、更新日志、存档详情等二级页现在可用安卓系统返回手势 / 返回键回到上一级（接入 `@capacitor/app`），不再只能点页面里的返回按钮。
- ✨ **二级页打开 / 返回加入丝滑动效**：进入下一级从右侧滑入、返回上一级向右滑出、同级切换淡入；遵循系统「减少动态效果」无障碍设置。

> 键盘修复属于安卓原生行为，安装后请在真机上验证一次输入体验。

---

## v1.2.0 · 时间线搜索 + 照片存储升级 + 安卓键盘彻底修复 + 全新档案图标 — 2026-06-03

这一版聚焦「用得久、找得到、装得下」：时间线能搜索和按场景筛选，照片从 localStorage 迁到 IndexedDB 彻底摆脱约 5MB 上限，安卓输入法不再把整页顶飞，并把品牌图标重构成更有档案感的「归档文件夹」。

- 🔍 **时间线搜索 + 场景筛选**：时间线顶部新增搜索框（按描述 / 物品 / 备注实时搜索）和一条横向场景筛选条（全部 / 各场景一键筛选），存档多了也能马上找到。
- 🗄️ **照片存储升级到 IndexedDB**：存档数据从 localStorage（约 5MB 上限）迁移到 IndexedDB（容量随设备可用空间，通常数百 MB～数 GB），不再因照片多而存不下。**首次启动自动迁移旧数据，无需任何操作、不丢数据**；设置 →「数据」新增存储用量显示。拍照与相册照片现在统一压缩，更省空间。
- ⌨️ **彻底修复安卓「聚焦输入框时整页被顶到看不见」**：根因是上一版 adjustNothing + JS 手动顶起在高分屏机型上会滚过头。改回系统标准的 `adjustResize`，由系统收缩 WebView、自动保持输入框可见，并删除会滚飞的脚本。**仅影响安卓，桌面端无变化。**
- 🎨 **全新档案风图标 + Logo**：把「卡片堆」重构成更有档案感的「归档文件夹」（标签页 + 归档记录线），延续蓝紫渐变、提升品牌质感；桌面图标、安卓自适应图标、启动图与应用内 Logo 全部同步更新。

> 键盘修复属于安卓原生行为，安装后建议在真机上验证一次输入体验。

---

## v1.1.2 · 移动端布局稳定 + 更新日志收纳 + 回滚整理 + 图标提亮 — 2026-06-02

这一版继续打磨手机端：现实对比不再被长描述撑宽，设置页加入可折叠的版本更新日志，回滚入口更容易定位旧存档，同时把偏黑的 App icon 换成更明亮的蓝紫配色。

- 📐 **修复现实对比横向缩放与底栏漂移**：长版本描述现在会在选择框内省略显示，不再撑宽页面；根页面同时禁止横向滚动，底栏在上下滚动和横向滑动尝试时都保持固定。
- 🗂 **设置内新增更新日志二级页**：在「设置 → 关于」中进入「更新日志」，可逐个展开或收起各版本说明；最新版本默认展开，历史版本保持紧凑。
- ↩️ **支持系统级侧滑返回**：更新日志使用独立历史路由，安卓系统侧滑返回或页面内返回按钮都可以回到上一级设置菜单。
- ⏮️ **回滚选项栏重新整理**：保留原有应用内底部选择面板，入口改为左侧选择场景、右侧选择时间描述，长列表更容易定位。
- 🎨 **App icon 提亮**：保留 Life Archive 的卡片堆造型，把偏黑底色换成更明亮的蓝紫渐变，并同步桌面图标、安卓自适应图标和启动图。

---

## v1.1.1 · 移动端交互焕新 + 安卓键盘原生修复 — 2026-06-02

这一版聚焦安卓真机上的日常手感：修正上一版仍未切断的原生键盘窗口重排，并把常用选择交互统一成更适合手机的应用内组件。

- ⌨️ **真正修复安卓输入框聚焦跳动**：上一版设置的 Capacitor `Keyboard.resize` 实际只对 iOS 生效。现在 Android APK 构建时会给 Activity 强制写入 `windowSoftInputMode="adjustNothing"`，键盘弹出不再 resize 或平移整个 WebView；页面只在当前输入框被遮住时小幅上移。
- ➕ **底部中央大号新增按钮**：移动端底栏调整为「时间线 / 现实对比 / 新建存档 / 回滚 / 分支决策」，正中间用突出的大号渐变「＋」承载最高频记录入口。
- 🍽 **饮食 / 物品左右分段选择**：新建存档不再把两大组全部平铺；先选「饮食」或「物品场景」，再展示对应图标网格，页面更短、更整洁。
- 🎛 **选项框统一焕新**：现实对比、回滚和分支复盘中的原生下拉全部换成统一的应用内底部选择面板，避免安卓系统弹出巨大白色单选框。
- 👆 **清理长按蓝色高亮**：禁用 WebView 默认蓝色触摸高亮和长按选区，为按钮、卡片、选项增加轻微缩放与背景反馈；键盘导航仍保留可见焦点。

---

## v1.1.0 · 饭迹：饮食生活档案 + 安卓输入框跳动修复 — 2026-06-02

把「每天吃了什么」做成 Life Archive 的一个新维度——不是记账式饮食软件，而是**低成本记录 + 自动按天整理**的饮食生活档案。第一版专注「记录 + 时间线」。

- 🍽 **新增「饮食」场景**：早餐 / 午餐 / 晚餐 / 夜宵 / 零食饮品，配统一扁平图标。和原来的物品场景共用一套存档/对比/回滚逻辑，零学习成本。
- 🎴 **场景选择器分组**：新建存档时分成「🍽 饮食」和「📦 物品场景」两组，饮食放最前，一眼就能记一餐。
- ✍️ **表单随场景自适应**：选了餐次后，提示语变成食物示例、「物品清单」变「吃了什么」，记一顿饭更顺手。
- 📅 **时间线每日餐数**：当天有用餐记录时，日期标题右侧显示「🍽 N 餐」，一眼看出今天吃得规不规律。
- 🤖 **AI 拍照识别饭菜**：拍一张饭菜照，AI 自动选好餐次、把每样食物填进清单（用你自己的免费智谱 Key，仅存本地）。
- ⌨️ **修复安卓「聚焦输入框时页面/顶栏乱跳」**：根因是键盘弹出时 WebView 整体 resize 触发顶栏闪跳。改为键盘不再 resize 视口——打字时收起底部标签栏、给内容垫出键盘高度、并把当前输入框自动滚到可见区。**仅影响安卓，桌面端无变化。**

> 升级后用「设置 → 数据」里没有的话也行：直接到「新建存档」就能看到饮食场景；想看示例可在空时间线点「载入示例数据」。

---

## v1.0.14 · 相机 + 场景选择器重做 + AI 识别场景 — 2026-06-02

- 📷 **调用相机**：新建存档点照片区，安卓会弹出原生选择（**拍照 / 从相册选**），不再只能进相册。（接入 `@capacitor/camera`，已声明相机权限）
- 🎴 **场景选择器重做**：把原生下拉换成**统一扁平图标的网格选择器**；分类更丰富（新增 厨房 / 衣柜 / 行李箱 / 车 / 钱包预算 / 抽屉…共 13 类）；时间线卡片和详情页的场景标签也统一成同款扁平图标。
- 🤖 **AI 顺带识别场景**：点「AI 识别照片」时，除了物品和描述，还会**自动帮你选好最贴切的场景**。

---

## v1.0.13 · 顶栏粘性 / 蓝色时间线 / 细节统一 — 2026-06-02

- 🐛 **修复顶栏下滑时跟着滚走/消失**：根因是 `html/body` 的 `height:100%` 破坏了粘性定位，已改为让页面自然增高。顶栏现在牢牢固定在顶部。**这同时改善了"聚焦输入框时页面上移"的问题。**
- 🟦 **恢复左侧蓝色时间线**（竖线 + 蓝点），统一设计审美。
- 🧭 **移动端去掉卡片右侧的 › 指示符**（桌面端保留，方便鼠标用户）。
- 🗑️ **详情页删除按钮加上统一底色**（不再是突兀的透明样式）。

---

## v1.0.12 · 详情页 / 时间线分栏 / 磨砂顶栏 等 — 2026-06-02

- 📄 **点击存档进入详情页**：大图 + 完整信息（场景/时间/物品/备注），**编辑 / 现实对比 / 回滚 / 删除**都在详情里。
- 🗑️ **删除移入详情页**，列表卡片上不再有删除按钮，**防误触**。
- 🗂️ **时间线按日期分栏**：今天 / 昨天 / 具体日期作为分组标题，卡片上显示所属场景，不再只能靠卡片看时间。
- 🎴 **顶栏 HyperOS 式磨砂**：底部渐变淡出，下滑时不再有硬分界线。
- 🐛 **修复"取消选照片后又弹一次相册"**（照片区域不再被包在 `<label>` 里导致的双触发）。
- ⌨️ **键盘弹出布局**：改用原生键盘 resize，底部标签栏不再乱跳 / 消失。
- ⚙ **清空全部 / 导出 JSON** 移到设置里（低频功能，避免误触）。

> 下个版本计划：调用相机拍照、场景选择器重做（统一图标 + 更多分类）+ AI 同时识别场景。

---

## v1.0.11 · 体验修复（编辑存档 / 输入法 / 顶栏 / 桌面对齐） — 2026-06-02

- ✏️ **存档可编辑**：每个存档卡片新增「编辑」，打开**预填好的表单**（场景/描述/照片/物品/备注），改完原地保存。
- ⌨️ **修复输入法弹出时布局错乱**：打字时自动隐藏底部标签栏，键盘收起后恢复。
- 🎴 **顶栏磨砂背景**：顶栏改为带主题色的半透明磨砂，下滑时不再出现难看的分界线。
- 🖥️ **桌面端标签对齐**：顶部导航 5 个标签改为**等宽**，中文长短不一也能整齐对齐。

---

## v1.0.10 · 修复安卓云同步失败 — 2026-06-02

- 🔧 修复：**安卓**上点同步报 `new row violates row-level security policy`（电脑端正常）。根因是为了让 AI 绕过跨域，之前在安卓上开了"全局把网络请求改走原生"，这会**丢掉 Supabase 的登录令牌（JWT）头**，服务器当成未登录而拒绝写入。
- 现改为：**只让智谱 AI 这一个请求单独走原生网络**，Supabase 和其它请求都走正常方式、令牌头完整 → 安卓云同步恢复正常。后端无需改动。

---

## v1.0.9 · 账号与云同步（Supabase，免费） — 2026-06-02

### 🆕 账号系统 + 多设备云同步
- 新增「设置 → 账号与云同步」：**邮箱注册 / 登录**，把存档同步到云端，多设备共享。
- 后端用 **Supabase**（免费额度）。你在设置里填**自己项目的 URL + anon key**（只存本机、可放心填），App 直连。
- 同步采用**按 id 合并**策略（本机与云端取并集，不丢数据）；登录后会自动同步一次，也可手动「立即同步」。
- 架构上做成**可随时换后端**：以后想换 CloudBase / 自建服务器，只改一处适配代码，App 主体和数据不变。

> 首次使用需要先在 Supabase 建项目并建一张表（建表 SQL 见仓库说明）。这是账号功能第一版，遇到报错把提示发我即可微调。

---

## v1.0.8 · 全面屏状态栏彻底修复 + 设置内切换语言/主题 — 2026-06-02

- 🔝 **状态栏（全面屏）**：之前「关闭强制全面屏 + StatusBar 插件」在部分机型（如 HyperOS）被忽略。改用读取**真实安全区高度**并注入 CSS 变量的方案（`@capacitor-community/safe-area`），顶栏与底栏自动避开状态栏/手势条，全面屏机型通用。
- ⚙ **设置整合**：语言（中文 / English）和主题（**跟随系统 / 浅色 / 深色**）都移到「设置 → 外观与语言」。顶栏不再放语言按钮；主题可手动锁定浅色或深色，不再只能跟随系统。

---

## v1.0.7 · 设置页（版本号/检查更新）+ 真正修复签名 + 状态栏 — 2026-06-02

### 🆕 设置页
- 顶栏新增 **⚙ 设置**：内置**当前版本号**、**检查更新**（发现新版可一键去下载页）、AI 密钥管理、数据导出/清空，以及「账号与云同步（开发中）」入口。

### 🔧 修复
- 🔑 **真正修复签名不一致**：之前把固定 keystore 拷到 `~/.android` 在云端不生效（每次还是随机签名）。改为在 `build.gradle` 里**显式指定固定 keystore 的绝对路径**，已验证签名固定为 `CF:92:CA…`。**装好这一版后，以后更新就不用再卸载了。**
- 🔝 **状态栏遮挡再加固**：除了主题层关闭强制全面屏，又加了 StatusBar 插件，启动时把内容压到状态栏下方、并跟随亮/暗设置状态栏颜色。

> ⚠️ 因为签名从这一版起才真正固定下来，**安装 v1.0.7 仍需先卸载旧版一次**；之后所有更新都能直接覆盖。

---

## v1.0.6 · AI 拍照识别（免费） — 2026-06-02

### ✨ 新功能
- **AI 拍照识别**：在新建存档里拍照后，点「✨ AI 识别照片」，自动识别照片里的物品并生成一句描述，填进表单——不用再手动输入一堆。
- **零成本**：用智谱 AI 的免费模型 `GLM-4V-Flash`；你在 App 里填**自己申请的免费 API Key**（只存手机本地、不上传、不打进安装包），无需服务器、不产生费用。
- **怎么用**：① 去 bigmodel.cn 注册并实名，免费拿一个 API Key → ② App 里拍照 → 点「AI 识别照片」→ 第一次会让你粘贴 key → ③ 之后自动识别。

> 这是 AI 功能的第一版；如果识别报错，把提示发我，我据此微调。账号 / 云端同步在后续版本做。

---

## v1.0.5 · 修复状态栏遮挡、更新需卸载、新建存档简化 — 2026-06-02

### 🔧 修复
- 🔝 **顶栏不再顶进状态栏**：关闭安卓 15 的强制全面屏（edge-to-edge），状态栏正常留白。
- 🔑 **修复每次更新都要卸载重装**：所有安卓包改用**固定签名**，以后新版可直接覆盖安装。
  ⚠️ 本版因为签名从“每次随机”换成“固定”，安装时仍需**卸载旧版一次**；从 1.0.5 起以后都不用了。

### ✨ 改进
- 📷 **新建存档更轻量**：改成“**拍张照 + 一句话**”就能存；物品清单 / 备注收进可选的展开项，不必每次填一堆。

---

## v1.0.4 · 顶部安全区适配 + 扁平化图标 + 新 logo — 2026-06-02

- 🔝 **适配全面屏**：顶栏增加顶部安全区内边距，不再顶进手机状态栏（信号/时间）里。
- 🎯 **扁平化 tab 图标**：底部标签栏改用统一的扁平线条图标（取代之前的 emoji），未选中为灰色、选中变蓝，更符合主流审美。
- 🪪 **新 logo**：应用内左上角图标换成与 App 图标一致的「卡片堆」（之前还是旧的分支符号）。

---

## v1.0.3 · 界面全面重做（HyperOS 风格）+ 修复安卓版本号 — 2026-06-02

### 🎨 全新界面（HyperOS 风格）
- 贴合小米 HyperOS 设计语言：浅灰背景 + 白色大圆角卡片 + 粗大标题 + 蓝色强调。
- 亮色 / 暗色双主题，跟随系统深色模式自动切换。
- 手机端底部标签栏；桌面端顶部胶囊式分段导航；窗口背景跟随系统亮/暗。
- Windows 与 Android 全平台统一新风格。

### 🔢 修复
- 修复安卓上版本号始终显示 1.0 的问题，现在正确显示实际版本号（构建时从 `package.json` 自动同步 `versionName`/`versionCode`）。

---

## v1.0.2 · 移动端体验重做 + 彻底改名 — 2026-06-01

### 📱 移动端体验重做
- **底部标签栏**：此前顶部 5 个标签在手机上需要 599px，而屏幕只有 375px，导致「回滚」「分支决策」被挤到屏幕外、很难发现。现在导航改为固定在底部的标签栏（图标在上、文字在下），5 个标签一屏全显示、拇指可达。
- **输入框 16px**，避免在手机上点输入框时页面自动缩放。
- **更大的点击区域**，内容底部留白避免被底栏遮挡。
- **刘海屏 / 底部安全区适配**（safe-area）。
- 桌面端（≥720px）布局保持不变。

### 🔖 彻底改名
- appId 由 `com.realitygit.app` 改为 `com.lifearchive.app`。
- GitHub 仓库、npm 包名、本地存储键等所有旧标识，从 RealityGit 统一改为 Life Archive。

> ⚠️ 因 appId 变更，装了 v1.0.0 / v1.0.1 旧版的设备会把本版当成「另一个 App」，不会自动更新到 v1.0.2，需手动安装一次。

---

## v1.0.1 · 改名 Life Archive + 全新图标 + 安卓首发 — 2026-06-01

- 🏷️ **正式定名 Life Archive / 生活存档**：应用显示名、窗口标题、安装包名全部更新。
- 🎨 **全新 App 图标**：蓝紫渐变的「存档卡片堆」，取代旧的 git 分支图标。
- 🤖 **新增安卓 APK**：首个移动端版本，带自适应图标。
- 📖 **GitHub 门面**：README 增加功能截图、徽章与下载说明。
- 🛠️ 一键发布脚本与自动发布流程。

> 内部 appId 此版本仍为 `com.realitygit.app`。

---

## v1.0.0 · 首个发布（原名 RealityGit） — 2026-06-01

首个可用版本。给现实生活装一个 Git —— 存档、对比、回滚、分支。

- 🌳 **时间线**：按场景分组的 git 式生活存档时间线。
- 🔍 **现实对比**：照片像素热力图 + 物品清单 diff（多了 / 少了 / 数量变化）。
- ⏮️ **回滚**：按旧存档生成分步恢复清单。
- 🔀 **分支决策**：开分支、记录预期、事后复盘评分。
- 🌐 中英双语 · 本地存储 · 离线可用。
- 🪟 Windows 安装版 + 免安装版，内置自动更新。
